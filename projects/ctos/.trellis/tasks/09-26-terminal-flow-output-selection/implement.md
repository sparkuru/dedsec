# 终端入口、输入与输出选择：执行计划

1. 读取本任务 PRD/设计、`.trellis/spec/frontend/index.md`、共享思考指南和相关终端源码；确认工作区与设备状态。实现前先搜索即将改动的标签、快捷键和输入配置的所有引用。
2. 将终端页拆成未连接选择态和已连接会话态；处理启动/关闭防重入、Root 不可用和模式显示。保留原生会话调用与尺寸回调。
3. 将 `TerminalView` 设为只读，替换底部输入路径为普通文本 IME 的实时 PTY 输入；统一处理输入、退格、组合输入、粘贴、发送/回车和五个快捷键。删掉三条预设命令。
4. 从 xterm 已渲染缓冲区生成快照，在会话顶部“换用”右侧加入“选择输出”入口与独立子页面，核实长文本滚动、选择、复制和返回时会话连续。清空输入框提示，移除终端欢迎语，并收敛工作台、设备、网络、命令、连接页的演示式文案。
5. 增加有判别力的 Flutter 组件/输入测试；运行 `flutter analyze`、相关 `flutter test`，按影响范围构建 APK。若构建可用，在授权设备上以 `adb -s 192.168.9.9:45075` 验证普通键盘、Shell 补全/历史、Ctrl-C、输出选择和会话切换；记录无法覆盖的机型/权限状态。
6. 更新 `design/architecture.md`、`design/changelog.md`、`design/verification.md` 及必要的索引/兼容性说明，严格区分实现与实测；检查最终 diff 和 Trellis 质量门禁，再按项目流程提交与收尾。

## 检查点

- 输入路径完成后，先验证 PTY 事件顺序和输入法组合输入，再做界面与文档；发现平台差异可回退该组件而不动原生代码。
- 设备验证会执行当前 ctOS 的终端命令，仅在本任务范围内使用显式序列号；不修改 Root 管理器、Vector 配置或其他应用。

## 执行与检查结果（2026-09-26）

- `./hako flutter analyze` 无问题，`./hako flutter test` 22/22 通过，`git diff --check` 通过；`./hako current` 构建并校验签名与 `SHA256SUMS`。当前 APK SHA-256：`1a610e33fc018d20f4b99161d6eb5e91f4eb594930f0d0033e3eea5c873245ae`。
- PLR110 `192.168.9.9:45075` 覆盖安装成功，设备 `base.apk` 哈希一致。当前包在 App Shell 中，`ec` 按 Tab 后上下同步为 `echo`；按用户原步骤输入 `i` 再按 Tab，上下都保留 `i` 并显示多个 Shell 候选；继续输入 `d` 并点键盘发送后，`id` 执行一次并返回应用 UID。执行 `echo hello` 后 ↑ 恢复完整命令、↓ 恢复空草稿；“选择输出”子页显示当前快照。选择复制、普通文本 IME 类型、中文候选、退格、Root PTY 的 `id` 及关闭回入口来自此前 `da530c09…` 包的实测，不算作当前包逐项通过。详见 [验证记录](../../../design/verification.md)；Android 11 和其他输入法尚未在当前包重测。
- 核查过程中发现百度输入法候选提交会替换已有拼音；输入组件改为保留编辑状态并向 PTY 发送 Unicode 差量。用户后续指出 Tab 后输入框内容消失，现根据 Shell 已渲染行同步到底部；↑/↓维护本会话由底部提交的命令，避开目标 App Shell 的长行历史重绘故障。
