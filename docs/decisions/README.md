# 架构决策记录

只记录影响多个实验或项目、且未来可能被重新评估的决策。

文件名采用 `NNNN-short-title.md`，正文包含：

1. 状态：proposed、accepted、superseded 或 rejected。
2. 背景与约束。
3. 决策。
4. 被放弃的候选及原因。
5. 结果与验证方式。
6. 重新评估条件。

单个宿主版本的字段名、方法签名和 DexKit 特征属于项目适配资料，不写入此目录。
